import { ElerefDirective } from './eleref.directive';

describe('ElerefDirective', () => {
  it('should create an instance', () => {
    const directive = new ElerefDirective();
    expect(directive).toBeTruthy();
  });
});
